const { fetchUnifiedCustomProduct, fetchUnifiedCustomProducts } = require('./src/utils/woocommerce-custom-unified');
// wait we need to run it in nextjs environment or map env variables. 
// It's easier to hit the local nextjs api.
